# 20260428.018 - Проверка переменных в промптах и state

**Дата:** 2026-04-28  
**Автор:** AI Agent (Claude)  
**Задача:** Проверить, что все переменные из промптов устанавливаются в state перед использованием

## 🎯 Общий результат

✅ **ВСЕ ПЕРЕМЕННЫЕ КОРРЕКТНО УСТАНАВЛИВАЮТСЯ В STATE**

- Создана функция `_get_available_tools()` для генерации JSON инструментов
- Добавлен `state['available_tools']` во все 5 nodes
- Все остальные переменные из промптов корректно передаются через state

---

## 📋 Таблица переменных по узлам

### 1. plan_node

**Промпты:**
- `prompts/plan/prompt.md`
- `prompts/plan/system.md` (включает `system_prompt_base.md`)
- `prompts/plan/user.md`

**Используемые переменные:**
| Переменная | Где используется | Где устанавливается | Статус |
|------------|------------------|---------------------|--------|
| `available_tools` | plan/prompt.md, system_prompt_base.md | plan_node:478 | ✅ |
| `messages` | plan/prompt.md | из state (история) | ✅ |
| `user_query` | plan/user.md | initial_state → state | ✅ |
| `system_prompt` | через include | plan_node:476 | ✅ |
| `MAX_ITERATIONS` | system.md | plan_node:477 | ✅ |
| `step` | system.md | plan_node:515 | ✅ |

**Код установки:**
```python
# plan_node (строки 476-478)
state['system_prompt'] = _SYSTEM_PROMPT
state['MAX_ITERATIONS'] = MAX_ITERATIONS
state['available_tools'] = _get_available_tools()
```

---

### 2. action_node

**Промпты:**
- `prompts/action/prompt.md`
- `prompts/action/system.md` (включает `system_prompt_base.md`)
- `prompts/action/user.md`

**Используемые переменные:**
| Переменная | Где используется | Где устанавливается | Статус |
|------------|------------------|---------------------|--------|
| `available_tools` | action/prompt.md, system_prompt_base.md | action_node:559 | ✅ |
| `iteration` | action/system.md | plan_node:516 → action_node:546 | ✅ |
| `MAX_ITERATIONS` | action/system.md | action_node:558 | ✅ |
| `refinement_plan` | action/system.md | refine_node:920 | ✅ |
| `plan` | action/system.md | plan_node:517 | ✅ |
| `all_tool_results` | action/system.md, user.md | action_node:704 | ✅ |
| `user_query` | action/user.md | initial_state → state | ✅ |
| `step` | action/system.md | action_node:605 | ✅ |
| `messages` | action/prompt.md | plan_node:518 → action_node:581 | ✅ |

**Код установки:**
```python
# action_node (строки 557-559)
state['system_prompt'] = _SYSTEM_PROMPT
state['MAX_ITERATIONS'] = MAX_ITERATIONS
state['available_tools'] = _get_available_tools()
```

---

### 3. observation_node

**Промпты:**
- `prompts/observation/prompt.md`
- `prompts/observation/system.md` (включает `system_prompt_base.md`)
- `prompts/observation/user.md`

**Используемые переменные:**
| Переменная | Где используется | Где устанавливается | Статус |
|------------|------------------|---------------------|--------|
| `available_tools` | system_prompt_base.md (через include) | observation_node:776 | ✅ |
| `iteration` | observation/system.md, user.md | action_node:546 | ✅ |
| `MAX_ITERATIONS` | observation/system.md | observation_node:775 | ✅ |
| `step` | observation/system.md | observation_node:828 | ✅ |
| `user_query` | observation/user.md | initial_state → state | ✅ |
| `tools_json` | observation/user.md | observation_node:777 | ✅ |
| `tool_calls` | observation/prompt.md | action_node:606-609 | ✅ |
| `messages` | observation/prompt.md | action_node:581 | ✅ |

**Код установки:**
```python
# observation_node (строки 774-777)
state['system_prompt'] = _SYSTEM_PROMPT
state['MAX_ITERATIONS'] = MAX_ITERATIONS
state['available_tools'] = _get_available_tools()
state['tools_json'] = tools_json
```

---

### 4. refine_node

**Промпты:**
- `prompts/refine/prompt.md`
- `prompts/refine/system.md` (включает `system_prompt_base.md`)
- `prompts/refine/user.md`

**Используемые переменные:**
| Переменная | Где используется | Где устанавливается | Статус |
|------------|------------------|---------------------|--------|
| `available_tools` | system_prompt_base.md (через include) | refine_node:877 | ✅ |
| `iteration` | refine/system.md | action_node:546 | ✅ |
| `MAX_ITERATIONS` | refine/system.md | refine_node:876 | ✅ |
| `step` | refine/system.md | refine_node:918 | ✅ |
| `user_query` | refine/user.md | initial_state → state | ✅ |
| `observation` | refine/user.md | observation_node:829 | ✅ |
| `all_tool_results` | refine/user.md | action_node:704 | ✅ |
| `messages` | refine/prompt.md | observation_node:830 | ✅ |

**Код установки:**
```python
# refine_node (строки 875-877)
state['system_prompt'] = _SYSTEM_PROMPT
state['MAX_ITERATIONS'] = MAX_ITERATIONS
state['available_tools'] = _get_available_tools()
```

---

### 5. final_node

**Промпты:**
- `prompts/final/prompt.md`
- `prompts/final/system.md` (включает `system_prompt_base.md`)
- `prompts/final/user.md`

**Используемые переменные:**
| Переменная | Где используется | Где устанавливается | Статус |
|------------|------------------|---------------------|--------|
| `available_tools` | system_prompt_base.md (через include) | final_node:987 | ✅ |
| `total_tools` | final/system.md, user.md | final_node:988 | ✅ |
| `iteration` | final/system.md | action_node через refine_node:942 | ✅ |
| `step` | final/system.md | final_node:1045 | ✅ |
| `user_query` | final/user.md | initial_state → state | ✅ |
| `plan` | final/user.md | plan_node:517 | ✅ |
| `observation` | final/user.md | observation_node:829 | ✅ |
| `all_results_json` | final/user.md | final_node:989 | ✅ |
| `messages` | final/prompt.md | observation_node:830 | ✅ |
| `MAX_ITERATIONS` | final/system.md | final_node:986 | ✅ |

**Код установки:**
```python
# final_node (строки 984-989)
state['system_prompt'] = _SYSTEM_PROMPT
state['MAX_ITERATIONS'] = MAX_ITERATIONS
state['available_tools'] = _get_available_tools()
state['total_tools'] = total_tools
state['all_results_json'] = all_results_json
```

---

## 🆕 Добавлена функция `_get_available_tools()`

Создана новая функция для генерации JSON списка инструментов:

```python
def _get_available_tools() -> str:
    """
    Возвращает форматированный JSON со списком доступных инструментов.
    
    Используется для передачи в state['available_tools'] перед генерацией промптов.
    """
    tools_json_compact = _build_tools_json(compact=True)
    # Форматируем JSON для читаемости
    try:
        tools_list = json.loads(tools_json_compact)
        # Красиво форматируем: каждый инструмент на новой строке
        tools_formatted = "[\n  " + ",\n  ".join(
            json.dumps(t, ensure_ascii=False, separators=(',', ':'))
            for t in tools_list
        ) + "\n]"
    except Exception:
        tools_formatted = tools_json_compact
    
    return tools_formatted
```

**Преимущества:**
- ✅ DRY принцип - логика в одном месте
- ✅ Легко поддерживать и изменять
- ✅ Консистентность форматирования во всех nodes

---

## 📝 Изменения в коде

### 1. Упрощена `_load_system_prompt()`

**Было:**
```python
def _load_system_prompt() -> str:
    # ... 15 строк кода с форматированием JSON ...
    state = {'available_tools': tools_formatted}
    prompt = prompts.get_system_prompt_base(state)
    return prompt
```

**Стало:**
```python
def _load_system_prompt() -> str:
    state = {
        'available_tools': _get_available_tools()  # ← использует новую функцию
    }
    prompt = prompts.get_system_prompt_base(state)
    return prompt
```

### 2. Добавлен `available_tools` во все nodes

**plan_node (строка 478):**
```python
state['available_tools'] = _get_available_tools()  # ← ДОБАВЛЕНО
```

**action_node (строка 559):**
```python
state['available_tools'] = _get_available_tools()  # ← УЖЕ БЫЛО
```

**observation_node (строка 776):**
```python
state['available_tools'] = _get_available_tools()  # ← ДОБАВЛЕНО
```

**refine_node (строка 877):**
```python
state['available_tools'] = _get_available_tools()  # ← ДОБАВЛЕНО
```

**final_node (строка 987):**
```python
state['available_tools'] = _get_available_tools()  # ← УЖЕ БЫЛО
```

---

## 🔍 Детальный анализ переменных в промптах

### system_prompt_base.md

**Используемые переменные:**
- `{{ available_tools }}` - ✅ установлен во всех 5 nodes

**Включается в:**
- plan/system.md
- action/system.md
- observation/system.md
- refine/system.md
- final/system.md

### plan промпты

**plan/prompt.md:**
```jinja2
[SYSTEM]
{% include 'plan/system.md' %}

[TOOLS]
{{ available_tools }}  ← ✅

[MESSAGES]
{{ messages }}  ← ✅

[USER]
{% include 'plan/user.md' %}
```

**plan/system.md:**
```jinja2
{% include 'system_prompt_base.md' %}  ← ✅ available_tools

ТЕКУЩИЙ ЭТАП: plan

⚠️ ОБЯЗАТЕЛЬНЫЙ ФОРМАТ ОТВЕТА для этапа plan:
{
  "status": "plan",
  "step": 1,  ← ✅
  "thought": "...",
  "plan": [...]
}
```

**plan/user.md:**
```jinja2
Вопрос пользователя:
{{ user_query }}  ← ✅
```

### action промпты

**action/prompt.md:**
```jinja2
[SYSTEM]
{% include 'action/system.md' %}

[TOOLS]
{{ available_tools }}  ← ✅

[MESSAGES - HISTORY]
{% for msg in messages %}  ← ✅
[{{ msg.role|upper }}]
{{ msg.content }}
{% endfor %}

[USER - CURRENT REQUEST]
{% include 'action/user.md' %}
```

**action/system.md:**
```jinja2
{% include 'system_prompt_base.md' %}  ← ✅ available_tools

ТЕКУЩИЙ ЭТАП: action (итерация {{ iteration }}/{{ MAX_ITERATIONS }})  ← ✅

План {% if refinement_plan %}уточнения{% else %}поиска{% endif %}:  ← ✅
{% if refinement_plan %}  ← ✅
{% for step in refinement_plan %}  ← ✅
...
{% else %}
{% for step in plan %}  ← ✅
...
{% endif %}
{% if iteration > 1 and all_tool_results %}  ← ✅

Выполнено ранее: {{ all_tool_results|length }} инструментов.  ← ✅
{% endif %}

{
  "status": "action",
  "step": {{ step + 1 }},  ← ✅
  ...
}
```

**action/user.md:**
```jinja2
Вопрос пользователя:
{{ user_query }}  ← ✅

{% if all_tool_results %}  ← ✅
⚠️ **IMPORTANTE: Ранее выполненные инструменты:**
{% for tr in all_tool_results %}  ← ✅
{{ loop.index }}. {{ tr.tool }}({{ tr.input | tojson }})
{% endfor %}
{% endif %}
```

### observation промпты

**observation/prompt.md:**
```jinja2
[SYSTEM]
{% include 'observation/system.md' %}

[TOOLS - ALREADY EXECUTED]
Tools executed in this iteration: {{ tool_calls|length }}  ← ✅

[MESSAGES - HISTORY]
{% for msg in messages[:-1] %}  ← ✅
[{{ msg.role|upper }}]
{{ msg.content }}
{% endfor %}

[USER - RESULTS TO ANALYZE]
{% include 'observation/user.md' %}
```

**observation/system.md:**
```jinja2
{% include 'system_prompt_base.md' %}  ← ✅ available_tools

ТЕКУЩИЙ ЭТАП: observation (итерация {{ iteration }}/{{ MAX_ITERATIONS }})  ← ✅

{
  "status": "observation",
  "step": {{ step + 1 }},  ← ✅
  ...
}
```

**observation/user.md:**
```jinja2
Вопрос пользователя:
{{ user_query }}  ← ✅

Результаты выполнения инструментов (итерация {{ iteration }}):  ← ✅
```json
{{ tools_json }}  ← ✅
```
```

### refine промпты

**refine/prompt.md:**
```jinja2
[SYSTEM]
{% include 'refine/system.md' %}

[MESSAGES - HISTORY]
{% for msg in messages %}  ← ✅
[{{ msg.role|upper }}]
{{ msg.content }}
{% endfor %}

[USER - CURRENT STATE]
{% include 'refine/user.md' %}
```

**refine/system.md:**
```jinja2
{% include 'system_prompt_base.md' %}  ← ✅ available_tools

ТЕКУЩИЙ ЭТАП: refine (итерация {{ iteration }}/{{ MAX_ITERATIONS }})  ← ✅

{
  "status": "refine",
  "step": {{ step + 1 }},  ← ✅
  ...
}
```

**refine/user.md:**
```jinja2
Вопрос пользователя:
{{ user_query }}  ← ✅

Текущая observation:
{{ observation }}  ← ✅

Всего найдено результатов: {{ all_tool_results|length }} инструментов  ← ✅
```

### final промпты

**final/prompt.md:**
```jinja2
[SYSTEM]
{% include 'final/system.md' %}

[MESSAGES - FULL HISTORY]
{% for msg in messages %}  ← ✅
[{{ msg.role|upper }}]
{{ msg.content }}
{% endfor %}

[USER - FINAL REQUEST]
{% include 'final/user.md' %}
```

**final/system.md:**
```jinja2
{% include 'system_prompt_base.md' %}  ← ✅ available_tools

ТЕКУЩИЙ ЭТАП: final

Всего выполнено {{ total_tools }} инструментов за {{ iteration }} итераций.  ← ✅

{
  "status": "final",
  "step": {{ step + 1 }},  ← ✅
  ...
}
```

**final/user.md:**
```jinja2
Вопрос пользователя:
{{ user_query }}  ← ✅

Первоначальный план:
{% for step in plan %}  ← ✅
{{ loop.index }}. {{ step }}
{% endfor %}

Финальная observation:
{{ observation }}  ← ✅

ВСЕ результаты инструментов ({{ total_tools }}) в формате JSON:  ← ✅
```json
{{ all_results_json }}  ← ✅
```
```

---

## 🎯 Полный список переменных (сводная таблица)

| Переменная | Plan | Action | Observation | Refine | Final | Откуда берется |
|------------|------|--------|-------------|--------|-------|----------------|
| `available_tools` | ✅ | ✅ | ✅ | ✅ | ✅ | `_get_available_tools()` |
| `user_query` | ✅ | ✅ | ✅ | ✅ | ✅ | `initial_state` |
| `step` | ✅ | ✅ | ✅ | ✅ | ✅ | Увеличивается в каждом node |
| `iteration` | — | ✅ | ✅ | ✅ | ✅ | plan_node:516, refine_node:942 |
| `MAX_ITERATIONS` | ✅ | ✅ | ✅ | ✅ | ✅ | Константа → state в каждом node |
| `system_prompt` | ✅ | ✅ | ✅ | ✅ | ✅ | `_SYSTEM_PROMPT` в каждом node |
| `messages` | ✅ | ✅ | ✅ | ✅ | ✅ | Накапливается в каждом node |
| `plan` | ✅ | ✅ | — | — | ✅ | plan_node:517 |
| `refinement_plan` | — | ✅ | — | — | — | refine_node:920 |
| `all_tool_results` | — | ✅ | — | ✅ | — | action_node:704 |
| `tool_calls` | — | — | ✅ | — | — | action_node:606-609 |
| `tools_json` | — | — | ✅ | — | — | observation_node:777 |
| `observation` | — | — | — | ✅ | ✅ | observation_node:829 |
| `total_tools` | — | — | — | — | ✅ | final_node:988 |
| `all_results_json` | — | — | — | — | ✅ | final_node:989 |

---

## ✅ Итоговый чеклист

### Переменные в state

- ✅ `available_tools` - добавлен во все 5 nodes через `_get_available_tools()`
- ✅ `system_prompt` - устанавливается в каждом node через `_SYSTEM_PROMPT`
- ✅ `MAX_ITERATIONS` - устанавливается в каждом node из константы
- ✅ `user_query` - передается через initial_state и сохраняется
- ✅ `step` - увеличивается в каждом node
- ✅ `iteration` - устанавливается в plan_node, увеличивается в refine_node
- ✅ `messages` - накапливается в каждом node
- ✅ `plan` - устанавливается в plan_node
- ✅ `refinement_plan` - устанавливается в refine_node
- ✅ `all_tool_results` - пополняется в action_node
- ✅ `tool_calls` - устанавливается в action_node
- ✅ `tools_json` - формируется в observation_node
- ✅ `observation` - устанавливается в observation_node
- ✅ `total_tools` - вычисляется в final_node
- ✅ `all_results_json` - формируется в final_node

### Функции

- ✅ `_get_available_tools()` - создана для генерации JSON инструментов
- ✅ `_load_system_prompt()` - упрощена, использует `_get_available_tools()`
- ✅ `_build_tools_json()` - существующая функция, используется в `_get_available_tools()`

### Nodes

- ✅ `plan_node` - добавлен `available_tools`
- ✅ `action_node` - `available_tools` уже был
- ✅ `observation_node` - добавлен `available_tools`
- ✅ `refine_node` - добавлен `available_tools`
- ✅ `final_node` - `available_tools` уже был

---

## 📊 Статистика изменений

**Изменено файлов:** 1
- `RAG/rag_lg_agent.py`

**Добавлено функций:** 1
- `_get_available_tools()`

**Изменено функций:** 1
- `_load_system_prompt()` (упрощена)

**Добавлено строк в nodes:** 4
- plan_node: +1 строка (available_tools)
- observation_node: +1 строка (available_tools)
- refine_node: +1 строка (available_tools)
- action_node, final_node: уже были

**Всего строк кода:** ~20

---

## 🧪 Тестирование

### ✅ Синтаксис Python

```bash
python -m py_compile rag_lg_agent.py
# OK - нет ошибок
```

### ⏳ Runtime тестирование (рекомендуется)

```bash
python rag_lg_agent.py "что такое smart monitor" --verbose
```

**Что проверить в логах:**
1. Файл `logs/002_llm_plan_request.log` - секция `[TOOLS]` должна содержать JSON с инструментами
2. Все 5 nodes должны корректно рендерить промпты
3. Переменные в промптах должны быть заполнены (не пустые `{{...}}`)

---

## 🔗 Связанные документы

- `20260428.017_fix_available_tools.md` - исходная проблема (available_tools не передавался)
- `20260428.007` - multi_term_exact_search (требует available_tools)
- `20260428.008` - score в semantic_search (требует available_tools)
- `20260428.009` - расширенная стратегия терминов (требует available_tools)

---

## 💡 Выводы

### Что было исправлено

1. ❌ **Проблема:** Функция `_get_available_tools()` не существовала
   - ✅ **Решение:** Создана функция с правильной логикой форматирования

2. ❌ **Проблема:** `available_tools` не передавался в plan_node, observation_node, refine_node
   - ✅ **Решение:** Добавлен во все 3 узла

3. ❌ **Проблема:** Дублирование кода форматирования JSON в `_load_system_prompt()`
   - ✅ **Решение:** Логика вынесена в `_get_available_tools()`, DRY принцип

### Почему это важно

**Без available_tools в промптах:**
- ❌ Агент НЕ видит список доступных инструментов
- ❌ Не знает параметры инструментов
- ❌ Может вызывать несуществующие инструменты
- ❌ Может использовать неправильные имена параметров

**С available_tools в промптах:**
- ✅ Агент видит полный список из 15 инструментов
- ✅ Знает точные имена параметров
- ✅ Может правильно планировать действия
- ✅ Формирует корректные JSON вызовы

### Качество кода

- ✅ DRY принцип - логика в одном месте
- ✅ Консистентность - одинаковый подход во всех nodes
- ✅ Читаемость - понятные имена функций
- ✅ Поддерживаемость - легко изменять один раз вместо пяти

---

## 🎊 Итог

**ВСЕ ПЕРЕМЕННЫЕ ИЗ ПРОМПТОВ КОРРЕКТНО УСТАНАВЛИВАЮТСЯ В STATE!**

Система готова к полноценной работе с правильной передачей всех параметров в промпты Jinja2.

**Следующие шаги:**
1. Runtime тестирование
2. Проверка логов LLM
3. Тестирование реальных запросов

---

**Статус:** ✅ Завершено и проверено (синтаксис)  
**Готово к:** Runtime тестированию

