# Параметризация скрипта транскрибации в t_directory.py

**Дата:** 2026-02-22  
**Файл:** `transcribe/t_directory.py`

## ✅ ИЗМЕНЕНИЯ

Добавлен параметр командной строки `--script` для указания скрипта транскрибации.

### До изменений:
```python
# Хардкод скрипта
t_gigaam_script = script_dir / "t_gigaam_simple.py"
```

### После изменений:
```python
# Параметр командной строки
parser.add_argument(
    "--script",
    default="t_gigaam_simple.py",
    help="Имя скрипта для транскрибации (по умолчанию: t_gigaam_simple.py)"
)

# Использование параметра
t_gigaam_script = script_dir / args.script
```

---

## 🎯 ИСПОЛЬЗОВАНИЕ

### По умолчанию (t_gigaam_simple.py):
```bash
python t_directory.py H:\videos
```

### С указанием другого скрипта:
```bash
# Использовать t_gigaam.py (с диаризацией спикеров)
python t_directory.py H:\videos --script t_gigaam.py

# Использовать t_gigaam_blocks.py (с блоками по времени)
python t_directory.py H:\videos --script t_gigaam_blocks.py

# Полная команда с параметрами
python t_directory.py C:\media --script t_gigaam.py --revision e2e_ctc --device cuda
```

---

## 📋 ДОСТУПНЫЕ СКРИПТЫ ТРАНСКРИБАЦИИ

| Скрипт | Описание | Особенности |
|--------|----------|-------------|
| `t_gigaam_simple.py` | Простая транскрипция | По умолчанию, без дополнительных функций |
| `t_gigaam_blocks.py` | С блоками по времени | Разбиение на блоки по паузам (MIN_PAUSE_SEC) |
| `t_gigaam.py` | С диаризацией спикеров | Определение спикеров через pyannote |

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Добавленные параметры:

```python
parser.add_argument(
    "--script",
    default="t_gigaam_simple.py",
    help="Имя скрипта для транскрибации (по умолчанию: t_gigaam_simple.py)"
)
```

### Валидация:

```python
t_gigaam_script = script_dir / args.script

if not t_gigaam_script.exists():
    logger.error(f"Не найден скрипт транскрибации: {t_gigaam_script}")
    sys.exit(1)
```

### Логирование:

```python
logger.info(
    f"Параметры обработки:\n"
    f"  Директория: {directory}\n"
    f"  Скрипт транскрибации: {args.script}\n"  # Новая строка
    f"  Ревизия: {args.revision}\n"
    f"  Устройство: {args.device}\n"
    f"  Пропускать существующие: {not args.force}\n"
)
```

---

## 📊 ПРИМЕРЫ ВЫВОДА

### Лог запуска:
```
2026-02-22 XX:XX:XX - INFO - ================================================================================
Параметры обработки:
  Директория: H:\videos
  Скрипт транскрибации: t_gigaam.py
  Ревизия: e2e_rnnt
  Устройство: auto
  Пропускать существующие: True
================================================================================
```

### Help команды:
```bash
python t_directory.py --help

positional arguments:
  directory             Директория для обработки

options:
  -h, --help            show this help message and exit
  --revision {e2e_rnnt,e2e_ctc,rnnt,ctc}
                        Ревизия модели GigaAM-v3
  --device {auto,cpu,cuda}
                        Устройство для обработки
  --force               Обрабатывать все файлы, даже если транскрипция уже существует
  --script SCRIPT       Имя скрипта для транскрибации (по умолчанию: t_gigaam_simple.py)
```

---

## ✨ ПРЕИМУЩЕСТВА

### ✅ Гибкость:
- Можно легко переключаться между разными скриптами транскрибации
- Не нужно редактировать код для изменения скрипта

### ✅ Удобство:
- По умолчанию используется самый простой скрипт
- Явное указание скрипта в логах

### ✅ Расширяемость:
- Легко добавлять новые скрипты транскрибации
- Единая точка обработки для всех скриптов

---

## 🎯 USE CASES

### 1. Быстрая транскрипция (без изысков):
```bash
python t_directory.py H:\videos
# Использует t_gigaam_simple.py
```

### 2. Транскрипция с временными блоками:
```bash
python t_directory.py H:\videos --script t_gigaam_blocks.py
# Разбивает на блоки по паузам
```

### 3. Транскрипция с определением спикеров:
```bash
python t_directory.py H:\videos --script t_gigaam.py
# Использует pyannote для диаризации
```

### 4. Пакетная обработка с выбором скрипта:
```bash
# Простая транскрипция всех файлов
python t_directory.py H:\lectures --script t_gigaam_simple.py --force

# Диаризация для интервью
python t_directory.py H:\interviews --script t_gigaam.py --device cuda

# Блоки для подкастов
python t_directory.py H:\podcasts --script t_gigaam_blocks.py
```

---

## 📝 ОБНОВЛЁННАЯ ДОКУМЕНТАЦИЯ

### Docstring:

```python
r"""
Скрипт для рекурсивной обработки медиафайлов в директории.

Обходит все папки и файлы в указанной директории,
для каждого медиафайла (аудио/видео) вызывает скрипт транскрибации
для получения транскрипции.

Usage:
    python t_directory.py <directory> [--script t_gigaam_simple.py] [--revision e2e_rnnt] [--device auto]
    python t_directory.py H:\videos --device cuda
    python t_directory.py C:\media --script t_gigaam.py --device cuda
    python t_directory.py C:\media --script t_gigaam_blocks.py --revision e2e_ctc --device cpu
"""
```

---

## ✅ ИТОГ

Параметр `--script` успешно добавлен в `t_directory.py`:
- ✅ По умолчанию: `t_gigaam_simple.py`
- ✅ Настраивается через командную строку
- ✅ Валидация существования скрипта
- ✅ Логирование выбранного скрипта
- ✅ Обновлена документация

**Теперь можно гибко выбирать скрипт транскрибации для пакетной обработки!** 🎉

