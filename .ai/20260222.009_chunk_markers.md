# Добавление маркеров склейки чанков [...] в скриптах транскрибации

**Дата:** 2026-02-22  
**Обновлено:** 2026-02-22 (добавлена проверка на пустые чанки)  
**Файлы:** `t_gigaam_simple.py`, `t_gigaam_blocks.py`, `t_gigaam.py`

## 🎯 ЗАДАЧА

Добавить маркер `[...]` в конец текста каждого чанка **только если чанк содержит текст**, чтобы видеть места склейки при последующей обработке текста.

## ✅ РЕАЛИЗАЦИЯ

### 1. `t_gigaam_simple.py` - Простая транскрипция

**Стало:**
```python
texts = []
for i, chunk in enumerate(chunking_result.chunks, 1):
    logger.info(f"→ Транскрипция чанка {i}/{len(chunking_result.chunks)} ({chunk.file_path.name})...")
    with torch.inference_mode():
        text = model.transcribe(str(chunk.file_path))
    # Добавляем маркер [...] только для непустых чанков
    chunk_text = text.strip()
    if chunk_text:  # ✅ Проверка на непустой текст
        chunk_text = chunk_text + " [...]"
        texts.append(chunk_text)
        logger.info(f"  {chunk_text[:80]}...")
    else:
        logger.debug(f"  Пропущен пустой чанк {i}")

full_text = " ".join(texts)
```

**Ключевое изменение:** Маркер `[...]` добавляется **только если** `chunk_text` не пустой.

---

### 2. `t_gigaam_blocks.py` - Транскрипция с блоками по паузам

**Стало:**
```python
def transcribe_single_chunk(model: AutoModel, chunk_path: Path, chunk_start_sec: float) -> str:
    """Транскрибирует один чанк аудио"""
    with torch.inference_mode():
        text = model.transcribe(str(chunk_path)).strip()
    # Добавляем маркер [...] только для непустых чанков
    if text:  # ✅ Проверка на непустой текст
        text_with_marker = text + " [...]"
        logger.debug(f"Транскрибировано символов: {len(text)}")
        return text_with_marker
    else:
        logger.debug("Пустой чанк, маркер не добавляется")
        return text
```

**Ключевое изменение:** Маркер добавляется **только если** `text` не пустой.

---

### 3. `t_gigaam.py` - Транскрипция с диаризацией спикеров

**Стало:**
```python
try:
    with torch.inference_mode():
        text = model.transcribe(str(chunk_path)).strip()

    if not text:
        continue

    # Разбиваем на предложения
    sentences = re.split(r'(?<=[.!?])\s+', text.strip())
    sentences = [s.strip() for s in sentences if s.strip()]

    # Добавляем маркер [...] только если есть предложения
    if sentences:  # ✅ Проверка на наличие предложений
        sentences[-1] = sentences[-1] + " [...]"
    
    # Собираем в блок с прикольным именем
    block_text = " ".join(sentences)
    
    # Добавляем блок только если есть текст
    if block_text:  # ✅ Дополнительная проверка
        block = TextBlock(
            start_sec=start,
            speaker=funny_name,
            text=block_text
        )
        all_blocks.append(block)
        logger.info(f"  {funny_name}: {block_text[:80]}...")
    else:
        logger.debug(f"  Пропущен пустой сегмент для {funny_name}")
    # ...
```

**Ключевое изменение:** Маркер добавляется **только если** есть предложения, и блок создаётся **только если** есть текст.

---

## 📊 РЕЗУЛЬТАТ

### Пример вывода с маркерами:

#### С непустыми чанками:
```
Это текст из первого чанка. Он продолжается здесь. [...] 
А это текст из второго чанка. Который тоже продолжается. [...] 
И текст из третьего чанка завершает все. [...]
```

#### Если есть пустые чанки (тишина):
```
Это текст из первого чанка. [...] 
Это текст из третьего чанка после тишины. [...] 
И последний чанк с текстом. [...]
```

**Примечание:** Пустые чанки (без текста) **пропускаются** - для них маркер `[...]` **НЕ добавляется**.

### Пример для диаризации (t_gigaam.py):
```
[00:00:00] 01.ПИКАЧУ: Здравствуйте, как дела? [...]

[00:00:15] 02.БУБЛИК: Отлично, спасибо! А у вас как? [...]

[00:00:30] 01.ПИКАЧУ: Тоже хорошо. Хотел обсудить проект. [...]
```

**Примечание:** Если сегмент спикера не содержит текста, он **не включается** в результат.

---

## 🎯 ПРЕИМУЩЕСТВА

### ✅ Видимость границ чанков:
- Явно видно, где заканчивается один чанк и начинается другой
- Можно отследить потенциальные проблемы со склейкой
- Помогает в отладке и проверке качества

### ✅ Пропуск пустых чанков:
- **Пустые чанки (тишина) не добавляют маркеры** - результат чище
- Нет лишних маркеров `[...]` в тексте
- Логирование пустых чанков в debug-режиме

### ✅ Последующая обработка:
- Легко найти и удалить маркеры: `.replace(' [...]', '')`
- Можно использовать маркеры для специальной обработки
- Разделение на чанки для анализа

### ✅ Контроль качества:
- Видно, если текст обрывается на середине предложения
- Можно оценить, насколько хорошо работает overlap
- Помогает настроить параметры нарезки

---

## 📝 КАК ИСПОЛЬЗОВАТЬ

### 1. Транскрибация с маркерами:
```bash
# Простая транскрипция
python t_gigaam_simple.py recording.wav

# С блоками
python t_gigaam_blocks.py recording.wav

# С диаризацией
python t_gigaam.py recording.amr
```

### 2. Чтение результата:
```python
# Чтение файла с маркерами
with open("recording.gigaam-e2e_rnnt-simple.txt", "r", encoding="utf-8") as f:
    text = f.read()

# Видим маркеры
print(text)
# "Текст чанка 1 [...] Текст чанка 2 [...] Текст чанка 3 [...]"
```

### 3. Удаление маркеров (если нужно):
```python
# Убираем маркеры
clean_text = text.replace(" [...]", "")
```

### 4. Разделение по чанкам (для анализа):
```python
# Разделяем текст обратно на чанки
chunks = text.split(" [...] ")
print(f"Количество чанков: {len(chunks)}")

for i, chunk in enumerate(chunks, 1):
    print(f"Чанк {i}: {chunk[:50]}...")
```

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Размещение маркеров:

| Скрипт | Место добавления маркера | Примечание |
|--------|--------------------------|------------|
| `t_gigaam_simple.py` | После `text.strip()` | Добавляется к каждому чанку |
| `t_gigaam_blocks.py` | В `transcribe_single_chunk()` | Добавляется к транскрипции чанка |
| `t_gigaam.py` | В конец последнего предложения | Только если есть предложения |

### Формат маркера:
```python
marker = " [...]"  # Пробел + [...] + пробел при склейке
```

### Логика для диаризации:
```python
# Разбиваем на предложения
sentences = ["Первое предложение.", "Второе предложение.", "Третье."]

# Добавляем маркер к последнему предложению
sentences[-1] = sentences[-1] + " [...]"
# Результат: ["Первое предложение.", "Второе предложение.", "Третье. [...]"]

# Склеиваем
block_text = " ".join(sentences)
# "Первое предложение. Второе предложение. Третье. [...]"
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверка работы маркеров:

```bash
# 1. Транскрибируем короткий файл
python t_gigaam_simple.py short_audio.wav

# 2. Смотрим результат
cat short_audio.gigaam-e2e_rnnt-simple.txt

# Ожидается:
# "Текст первого чанка [...] Текст второго чанка [...] ..."
```

### Подсчёт чанков:
```python
with open("result.txt", "r", encoding="utf-8") as f:
    text = f.read()

chunk_count = text.count("[...]")
print(f"Количество чанков: {chunk_count}")
```

### Проверка склейки:
```python
# Ищем аномалии в склейке
lines = text.split(" [...] ")
for i, line in enumerate(lines):
    # Проверяем, что текст не обрывается
    if line and not line[-1] in ".!?":
        print(f"⚠️  Чанк {i+1} обрывается: ...{line[-50:]}")
```

---

## 📈 ПРИМЕРЫ ВЫВОДА

### t_gigaam_simple.py (20-сек чанки):
```
Это первый чанк транскрипции длительностью около двадцати секунд аудио. [...] 
Это второй чанк который продолжает текст и тоже содержит примерно двадцать секунд. [...] 
Это третий и последний чанк файла с оставшейся частью аудио. [...]
```

### t_gigaam_blocks.py (с блоками по паузам):
```
[00:00:00] Первый блок текста с несколькими предложениями. Они группируются по паузам. [...]

[00:01:30] Второй блок начинается после длинной паузы. Содержит новую мысль. [...]

[00:03:00] Третий блок завершает транскрипцию. [...]
```

### t_gigaam.py (с диаризацией):
```
[00:00:00] 01.ПИКАЧУ: Здравствуйте! Как ваши дела? [...]

[00:00:10] 02.БУБЛИК: Спасибо, отлично. А у вас? [...]

[00:00:20] 01.ПИКАЧУ: Тоже хорошо. Давайте обсудим детали проекта. [...]
```

---

## ⚙️ НАСТРОЙКА

### Изменение маркера (если нужно):

Если хотите другой маркер, измените код:

```python
# Было:
chunk_text = text.strip() + " [...]"

# Можно использовать:
chunk_text = text.strip() + " [CHUNK_END]"
chunk_text = text.strip() + " 🔸"
chunk_text = text.strip() + " <<<>>>"
```

### Отключение маркеров:

Просто закомментируйте добавление маркера или уберите `+ " [...]"`:

```python
# Без маркера:
chunk_text = text.strip()
```

---

## ✅ ИТОГ

**Маркеры `[...]` добавлены во все скрипты транскрибации:**

- ✅ `t_gigaam_simple.py` - после каждого чанка
- ✅ `t_gigaam_blocks.py` - в `transcribe_single_chunk()`
- ✅ `t_gigaam.py` - в конец последнего предложения сегмента

**Преимущества:**
- ✅ Видимость границ чанков
- ✅ Удобство для последующей обработки
- ✅ Контроль качества склейки
- ✅ Простота анализа

**Использование:**
```bash
python t_gigaam_simple.py audio.wav
# Результат: "текст чанка 1 [...] текст чанка 2 [...] ..."
```

**Готово к использованию!** 🎉

