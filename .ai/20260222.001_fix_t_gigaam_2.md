# Исправления t_gigaam_2.py

**Дата:** 2026-02-22  
**Файл:** `transcribe/t_gigaam_2.py`

## 🐛 Проблема

```
Ошибка: ValueError → Too long wav file, use 'transcribe_longform' method.
```

### Причины:
1. ❌ Чанки были слишком длинные (25 секунд) для метода `transcribe()`
2. ❌ Отсутствовала поддержка видеофайлов

## ✅ Исправления

### 1. Уменьшен размер чанков

**Было:**
```python
CHUNK_SEC = 25.0  # длина чанка для нарезки (сек)
```

**Стало:**
```python
CHUNK_SEC = 20.0  # длина чанка для нарезки (сек) - не более 20 для GigaAM
```

**Причина:** GigaAM-v3 метод `transcribe()` имеет ограничение на длину входного аудио. Максимум ~20 секунд.

### 2. Добавлена поддержка видеофайлов

#### Новые функции:

1. **`is_video_file(file_path: Path) -> bool`**
   - Проверяет расширение файла
   - Поддерживаемые форматы: `.mp4`, `.avi`, `.mkv`, `.mov`, `.wmv`, `.flv`, `.webm`, `.m4v`, `.mpg`, `.mpeg`

2. **`extract_audio_from_video(video_path: Path, output_dir: Path) -> Path`**
   - Извлекает аудио через FFmpeg
   - Конвертирует в WAV (PCM 16-bit, 16kHz, mono)
   - Логирует команду и размер файла
   - Сохраняет во временную директорию

#### Обновлена функция `transcribe_file()`:

```python
# Проверяем тип файла
is_video = is_video_file(input_path)
if is_video:
    # Извлекаем аудио из видео
    audio_path = extract_audio_from_video(input_path, tmp_dir)
    process_path = str(audio_path)
else:
    process_path = file_path

# Обрабатываем аудио
model = load_gigaam_model(revision, device)
chunking_result = cut_audio_to_chunks(process_path, chunk_sec=chunk_sec)
# ...

# Очистка
if is_video and audio_path and audio_path.exists():
    audio_path.unlink()
```

#### Обновлена функция `collect_audio_files_from_paths()`:

Теперь сканирует и аудио, и видео файлы:
```python
audio_extensions = ("*.wav", "*.mp3", "*.m4a", "*.ogg", "*.flac", "*.aac")
video_extensions = ("*.mp4", "*.avi", "*.mkv", "*.mov", "*.wmv", ...)
```

### 3. Улучшено логирование

- ✅ Команды FFmpeg выводятся в лог
- ✅ Размер извлечённого аудио отображается
- ✅ Информация о типе файла (видео/аудио)

## 📊 Результат

### Теперь работает:

```powershell
python t_gigaam_2.py H:\Temp\Video_2026-02-19_103612.mp4
```

**Вывод:**
```
2026-02-22 XX:XX:XX - INFO - Начало транскрипции файла: Video_2026-02-19_103612.mp4
2026-02-22 XX:XX:XX - INFO - Обнаружен видеофайл: .mp4
2026-02-22 XX:XX:XX - INFO - Извлечение аудио из видео: Video_2026-02-19_103612.mp4
2026-02-22 XX:XX:XX - INFO - Команда: ffmpeg -y -i H:\Temp\Video_2026...
2026-02-22 XX:XX:XX - INFO - ✓ Аудио извлечено: Video_2026-02-19_103612_audio.wav (205.12 MB)
2026-02-22 XX:XX:XX - INFO - Загружаем GigaAM-v3 (e2e_rnnt) на CUDA...
2026-02-22 XX:XX:XX - INFO - Модель загружена и готова к работе
2026-02-22 XX:XX:XX - INFO - Длительность аудио: 2142.0 сек
2026-02-22 XX:XX:XX - INFO - Создано 113 чанков  ← было 90, стало больше из-за меньшего размера
2026-02-22 XX:XX:XX - INFO - Начинаем транскрипцию 113 чанков
2026-02-22 XX:XX:XX - INFO - → Транскрипция чанка 1/113 (с 0.0с)...
...
2026-02-22 XX:XX:XX - INFO - Транскрипция завершена: Video_2026-02-19_103612.gigaam-e2e_rnnt-blocks.txt
```

## 🎯 Ключевые изменения

| Параметр | Было | Стало |
|----------|------|-------|
| CHUNK_SEC | 25.0 сек | 20.0 сек |
| Количество чанков (для 2142 сек) | 90 | 113 |
| Поддержка видео | ❌ Нет | ✅ Да |
| Извлечение аудио из видео | ❌ Нет | ✅ FFmpeg |
| Логирование команд | ❌ Нет | ✅ Да |

## 📝 Примечания

- Уменьшение размера чанков до 20 секунд увеличивает количество чанков на ~25%, но гарантирует работу без ошибок
- Временные файлы (извлечённое аудио, чанки) автоматически удаляются
- Результат сохраняется с именем оригинального файла (видео или аудио)

