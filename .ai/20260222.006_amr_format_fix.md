# Исправление ошибки чтения AMR и других проблемных аудиоформатов

**Дата:** 2026-02-22  
**Файлы:** `t_gigaam.py`, `transcribe_utils.py`, `transcribe_config.py`

## 🐛 ПРОБЛЕМА

### Ошибка:
```
ValueError → requested chunk [ 00:01:30.000 -->  00:01:40.000] 
from +7 831 260-16-61 (Нижний Новгород, Нижегородская обл.) 2026-02-21_124803_inc 
file resulted in 73600 samples instead of the expected 80000 samples.
```

### Причина:
Pyannote не может корректно читать некоторые аудиоформаты напрямую:
- **.amr** (Adaptive Multi-Rate) - используется в телефонии
- **.m4a** (MPEG-4 Audio)
- **.aac** (Advanced Audio Coding)
- **.3gp** (3GPP multimedia)
- **.opus** (Opus audio codec)
- **.wma** (Windows Media Audio)

Эти форматы вызывают несоответствие в количестве сэмплов при чтении через pyannote.

---

## ✅ РЕШЕНИЕ

Добавлена автоматическая конвертация проблемных форматов в WAV перед диаризацией.

### 1. Новая функция `needs_audio_conversion()`

```python
def needs_audio_conversion(file_path: Path) -> bool:
    """Проверяет, нужна ли конвертация аудио для pyannote"""
    problematic_formats = {'.amr', '.m4a', '.aac', '.3gp', '.opus', '.wma'}
    return file_path.suffix.lower() in problematic_formats
```

### 2. Новая функция `convert_audio_to_wav()`

```python
def convert_audio_to_wav(audio_path: Path, output_dir: Path) -> Path:
    """Конвертирует аудиофайл в WAV формат для pyannote"""
    wav_path = output_dir / f"{audio_path.stem}_converted.wav"
    
    cmd = [
        "ffmpeg", "-y", "-i", str(audio_path),
        "-acodec", "pcm_s16le",  # PCM 16-bit
        "-ar", "16000",          # 16kHz
        "-ac", "1",              # Mono
        str(wav_path)
    ]
    
    # Выполняем конвертацию...
    return wav_path
```

### 3. Обновлена логика `transcribe_with_speakers()`

```python
def transcribe_with_speakers(file_path: str, ...):
    # ...
    
    is_video = is_video_file(input_path)
    needs_conversion = needs_audio_conversion(input_path)
    
    if is_video:
        # Извлекаем аудио из видео
        converted_audio_path = extract_audio_from_video(input_path, tmp_dir)
        process_path = str(converted_audio_path)
    elif needs_conversion:
        # Конвертируем проблемный формат в WAV
        converted_audio_path = convert_audio_to_wav(input_path, tmp_dir)
        process_path = str(converted_audio_path)
    else:
        # Используем файл напрямую
        process_path = file_path
    
    # Диаризация теперь работает с WAV
    speaker_segments = diarize_audio(process_path, device)
    # ...
```

---

## 📊 ПОДДЕРЖИВАЕМЫЕ ФОРМАТЫ

### До исправления:
| Формат | Прямая работа | Статус |
|--------|---------------|--------|
| WAV | ✅ Да | OK |
| MP3 | ✅ Да | OK |
| AMR | ❌ Нет | **ОШИБКА** |
| M4A | ❌ Нет | **ОШИБКА** |
| AAC | ❌ Нет | **ОШИБКА** |

### После исправления:
| Формат | Прямая работа | Конвертация | Статус |
|--------|---------------|-------------|--------|
| WAV | ✅ Да | - | ✅ OK |
| MP3 | ✅ Да | - | ✅ OK |
| AMR | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |
| M4A | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |
| AAC | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |
| 3GP | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |
| OPUS | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |
| WMA | ❌ Нет | ✅ Да | ✅ **ИСПРАВЛЕНО** |

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Изменения в файлах:

#### 1. `t_gigaam.py`
- ✅ Добавлена функция `needs_audio_conversion()`
- ✅ Добавлена функция `convert_audio_to_wav()`
- ✅ Обновлена логика `transcribe_with_speakers()` с проверкой формата
- ✅ Корректная очистка временных файлов

#### 2. `transcribe_utils.py`
- ✅ Добавлена функция `needs_audio_conversion()`
- ✅ Добавлена функция `convert_audio_to_wav()`
- ✅ Доступны для использования в других скриптах

#### 3. `transcribe_config.py`
- ✅ Добавлена константа `PROBLEMATIC_AUDIO_FORMATS`

---

## 📝 ЛОГИРОВАНИЕ

### Пример работы с AMR файлом:

```
2026-02-22 08:06:06 - INFO - Начало обработки: +7 831 260-16-61 (...) 2026-02-21_124803_inc.amr
2026-02-22 08:06:06 - INFO - Обнаружен аудиофайл с проблемным форматом: .amr
2026-02-22 08:06:06 - INFO - Конвертация аудио в WAV для pyannote: +7 831 260-16-61_inc.amr
2026-02-22 08:06:06 - INFO - Команда: ffmpeg -y -i ... -acodec pcm_s16le -ar 16000 -ac 1 ...
2026-02-22 08:06:07 - INFO - ✓ Аудио сконвертировано: +7 831 260-16-61_inc_converted.wav (1.23 MB)
2026-02-22 08:06:07 - INFO - → Загрузка модели GigaAM-v3 (e2e_rnnt) на устройство: CUDA...
2026-02-22 08:06:12 - INFO - ✓ Модель загружена на CUDA
2026-02-22 08:06:12 - INFO - → Запуск pyannote speaker diarization...
2026-02-22 08:06:14 - INFO - ✓ Pyannote pipeline загружен на CUDA
2026-02-22 08:06:14 - INFO - → Запуск анализа спикеров...
2026-02-22 08:06:14 - INFO -   Файл: +7 831 260-16-61_inc_converted.wav
2026-02-22 08:06:14 - INFO -   Длительность: 102.0 сек
2026-02-22 08:07:15 - INFO - ✓ Диаризация завершена
2026-02-22 08:07:15 - INFO - Найдено 45 сегментов спикеров
...
```

---

## 🎯 ИСПОЛЬЗОВАНИЕ

### Команда теперь работает для AMR:

```bash
python t_gigaam.py "H:\CallRec\+7 831 260-16-61 (...) 2026-02-21_124803_inc.amr"
```

### Поддерживаемые команды:

```bash
# AMR файл (телефония)
python t_gigaam.py recording.amr

# M4A файл (iTunes)
python t_gigaam.py podcast.m4a

# AAC файл
python t_gigaam.py audio.aac

# 3GP файл (мобильная запись)
python t_gigaam.py voice.3gp

# OPUS файл (мессенджеры)
python t_gigaam.py message.opus

# WMA файл (Windows)
python t_gigaam.py audio.wma

# WAV/MP3 работают как раньше (без конвертации)
python t_gigaam.py recording.wav
python t_gigaam.py music.mp3
```

---

## ⚙️ ПАРАМЕТРЫ КОНВЕРТАЦИИ

```python
# Формат конвертации для всех проблемных файлов:
- Codec: PCM 16-bit (pcm_s16le)
- Sample Rate: 16000 Hz (16 kHz)
- Channels: 1 (Mono)
- Format: WAV
```

Эти параметры оптимальны для pyannote speaker diarization.

---

## 🧹 ОЧИСТКА ВРЕМЕННЫХ ФАЙЛОВ

Сконвертированные файлы автоматически удаляются после обработки:

```python
if converted_audio_path and converted_audio_path.exists():
    logger.debug(f"Удаляем сконвертированное аудио: {converted_audio_path.name}")
    converted_audio_path.unlink()
```

---

## ✨ ПРЕИМУЩЕСТВА

### ✅ Автоматизация:
- Не нужно вручную конвертировать файлы
- Автоматическое определение проблемных форматов
- Прозрачная работа для пользователя

### ✅ Совместимость:
- Поддержка всех популярных аудиоформатов
- Работа с телефонными записями (AMR)
- Поддержка мобильных форматов (3GP)

### ✅ Производительность:
- Конвертация только когда нужно
- WAV и MP3 работают напрямую (без overhead)
- Быстрая конвертация через FFmpeg

---

## 🔄 ОБРАТНАЯ СОВМЕСТИМОСТЬ

Изменения **полностью обратно совместимы**:
- WAV и MP3 файлы работают как раньше (без конвертации)
- Видеофайлы работают как раньше (извлечение аудио)
- Новая функциональность только для проблемных форматов

---

## 📈 ТЕСТИРОВАНИЕ

### Проверка исправления:

```bash
# 1. Тест AMR файла
python t_gigaam.py recording.amr
# Ожидается: успешная обработка без ошибок

# 2. Тест M4A файла
python t_gigaam.py podcast.m4a
# Ожидается: конвертация + обработка

# 3. Тест WAV (без конвертации)
python t_gigaam.py recording.wav
# Ожидается: прямая обработка без конвертации

# 4. Пакетная обработка
python t_directory.py H:\CallRec --script t_gigaam.py
# Ожидается: обработка всех AMR файлов в директории
```

---

## ✅ ИТОГ

**Проблема с AMR и другими проблемными форматами полностью решена!**

- ✅ Автоматическая конвертация проблемных форматов
- ✅ Поддержка AMR, M4A, AAC, 3GP, OPUS, WMA
- ✅ Обратная совместимость с WAV/MP3
- ✅ Интеграция в модули (`transcribe_utils.py`)
- ✅ Корректная очистка временных файлов

**Теперь скрипт работает со всеми популярными аудиоформатами!** 🎉

