
## ЭТАП: ACTION (Выполнение)

Твоя задача: **вызвать инструменты** согласно плану.

### 🎯 ВОПРОС ПОЛЬЗОВАТЕЛЯ (РЕАЛЬНЫЙ):

```
{{ user_query }}
```

⚠️ **Это НАСТОЯЩИЙ вопрос!** Вызови инструменты чтобы найти ответ на ЭТОТ конкретный вопрос.

### Текущий план

{% for step in plan %}
{{ loop.index }}. {{ step }}
{% endfor %}

### История вызовов

{% if all_tool_results %}
**Уже выполнено:**
{% for tr in all_tool_results %}
- {{ tr.tool }}({{ tr.input | tojson }})
{% endfor %}

⚠️ **НЕ повторяй** вызовы с теми же параметрами!
{% else %}
Это первая итерация.
{% endif %}

### Что делать

1. **Выбери инструменты** из плана
2. **Определи параметры** для каждого
3. **Сформируй actions**

### Formат ответа

```json
{{ schema_AgentAction }}
```

**НЕ используй** поля `observation`, `final_answer` на этом этапе.

### Важно

- **Точность**: проверь имена параметров в спецификации инструментов
- **Новое**: если это повторная итерация — ищи ДРУГУЮ информацию
